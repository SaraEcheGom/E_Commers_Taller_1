version: "3.8"

services:
  api:
    build: .
    container_name: ecommerce-api
    ports:
      - "8080:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped